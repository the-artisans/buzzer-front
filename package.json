{
  "name": "buzzer",
  "version": "1.0.0",
  "description": "Investmens simplified",
  "main": "index.html",
  "dependencies": {},
  "devDependencies": {
    "gulp": "^3.9.1",
    "gulp-concat": "^2.6.1",
    "gulp-csso": "^3.0.0",
    "gulp-less": "^3.3.0",
    "gulp-plumber": "^1.1.0",
    "gulp-serve": "^1.4.0",
    "gulp-sync": "^0.1.4",
    "less": "^2.7.2",
    "surge": "^0.19.0"
  },
  "scripts": {
    "build": "npm install && bower install && gulp build",
    "watch": "gulp watch",
    "start": "npm install && bower install && gulp build && gulp serve",
    "deploy": "surge ./"
  },
  "author": "",
  "license": "MIT"
}
